// Optional default settings. You may leave these blank and enter them on the main page.
window.ECG_REVIEW_CONFIG = {
  APP_VERSION: "1.0.0",
  // Example: "https://script.google.com/macros/s/AKfycb.../exec"
  APPS_SCRIPT_URL: "",
  // Prefer NOT to commit a real secret to a public GitHub repository.
  // You can provide a token on the main page or through ?token=... instead.
  SHARED_SECRET: ""
};
